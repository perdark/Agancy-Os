/**
 * Vitest stand-in for the `server-only` package, which throws when imported
 * outside a React Server Components module graph. Aliased in vitest.config.ts
 * so server modules stay unit-testable in Node. Intentionally empty.
 */
export {};
